// import _ from 'lodash';
// import { html } from '../dom';

const utils = {
	// TODO: create children flexible, when you didn't any child keyword (html, children, md, from,)
	// 	toChildren(children) {
	// 		if (_.isString(children)) {
	// 			// convert string html template to array of element object
	// 			return [];
	// 		}
	// 		if (_.isArray(children)) {
	// 			return children;
	// 		}
	// 		if (_.isObject(children)) {
	// 			return html.isElement(children) ? children : render(children);
	// 		}
	// 	},
};
export default utils;
